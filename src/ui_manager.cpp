#include "ui_manager.h"
#include <Arduino.h>

void UIManager::begin(int themeIndex) {
    (void)themeIndex;
}

void UIManager::drawBaseUI() {
    // Minimal placeholder: real drawing lives in refactor phases
}
